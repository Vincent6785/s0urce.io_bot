# Runtime artefacts
ocr-server/feedback.jsonl
autohack-feedback-*.jsonl
*.log
*.out

# Personal OCR dictionary export — training data, never committed.
# test/ocr_perf_test.js treats it as an optional fixture and skips without it.
bck

# Generated by an older test harness, no longer produced
*_mod.js

# The project has no dependencies; this only guards against accidents
node_modules/

# Editors and OS
.DS_Store
*~
*.swp
*.bak
